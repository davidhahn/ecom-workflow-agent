// beUI's Tabs component (beui.dev) imports EASE_OUT from this path, but the
// site only showed the import, not this file's contents. This is a
// standard ease-out-expo curve, not a verbatim pull from beUI's source.
export const EASE_OUT = [0.16, 1, 0.3, 1] as const;
