# Support Playbook

This document defines how incoming support tickets are categorized and routed for handling.

## Shipping

A support ticket categorized as `shipping` covers delivery delays, lost packages, and items damaged in transit. A `shipping` ticket is routed to <!-- TODO: clarify -->.

## Product Defect

A support ticket categorized as `product_defect` covers a product that is broken, malfunctioning, or otherwise not working as expected. A `product_defect` ticket is routed to <!-- TODO: clarify -->.

## Billing

A support ticket categorized as `billing` covers charge disputes, duplicate charges, and invoice requests. A `billing` ticket is routed to <!-- TODO: clarify -->.

## Other

A support ticket categorized as `other` covers any inquiry that does not fall under the `shipping`, `product_defect`, or `billing` categories, including general questions not tied to a specific order or product. An `other` ticket is routed to <!-- TODO: clarify -->.

<!-- TODO: clarify — the Part 1 support_tickets schema has no assignee, owner, or team field, so routing destinations are not currently modeled or enforceable structurally. The routing assignments in this document need either a schema addition or confirmation that routing is handled by a separate system. -->
